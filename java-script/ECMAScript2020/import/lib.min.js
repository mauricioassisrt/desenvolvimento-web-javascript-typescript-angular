export const saudacao = () => {
    let data = new Date().toLocaleDateString()
    return ` olá seja muito bem vindo ${data}`
}
export function getEnderecoByCEP(cep) {
    return fetch(`http://viacep.com.br/ws/${cep}/json`)
        .then(response => response.json())
}